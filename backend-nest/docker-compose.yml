version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: ibrc_db
    restart: always
    env_file:
      - .env
    ports:
      - '5432:5432'
    volumes:
      - ibrc_db_data:/var/lib/postgresql/data

volumes:
  ibrc_db_data:
